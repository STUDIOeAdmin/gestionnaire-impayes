# Base de données Neon (générée automatiquement par Vercel à la création du projet)
DATABASE_URL="postgresql://..."

# Secret NextAuth — générer avec : openssl rand -base64 32
AUTH_SECRET="votre-secret-ici"

# URL de l'app (en local : http://localhost:3000 ; en prod : https://votre-app.vercel.app)
NEXTAUTH_URL="http://localhost:3000"
AUTH_URL="http://localhost:3000"
